# Журнал обращений службы поддержки (SUPPORT_LOG.md)

| ID обращения | Тип | Описание обращения | Шаги воспроизведения / Контекст | Решение | Версия патча |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **#8** | Баг | Вылетает ошибка при вводе чисел с запятой (например, `90,5` вместо `90.5`). | 1. Выбрать обмен валюты.<br>2. Ввести сумму `100,50`.<br>3. Программа падает с ValueError. | В функции валидации чисел добавлен автоматический заменяющий фильтр `.replace(",", ".")`. | v1.0.1 |
| **#9** | Баг | Ошибка конвертации, если пользователь вводит сумму с пробелами-разделителями (например, `1 000`). | 1. Перейти в меню конвертации.<br>2. Ввести сумму `1 000`.<br>3. Система не распознает число. | Добавлена очистка входящей строки от пробелов через `.replace(" ", "")` перед кастом в float. | v1.0.1 |
| **#10** | Улучшение | Добавить автоматический расчет обратного курса при добавлении прямого. | При добавлении пары USD->RUB автоматически рассчитывать и предлагать добавить курс RUB->USD как `1 / курс`. | Функция `save_or_update_rate` дополнена опциональным флагом для автоматического сохранения обратного курса. | v1.0.2 |
| **#11** | Изменение | Ограничить вывод истории операций последними 5 записями. | При большом количестве транзакций история забивает весь терминал. | В SQL-запрос `get_all_history` добавлен параметр `LIMIT 5` для главного экрана истории. | v1.0.2 |
| **#12** | Вопрос | Где физически хранятся данные программы и как сделать бэкап? | Клиент хочет перенести историю операций на другой компьютер. | Добавлен подробный раздел FAQ в документацию проекта (README.md). | v1.0.2 |