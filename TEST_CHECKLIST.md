# Чек-лист приёмочного тестирования (Acceptance Test Checklist)

**Проект:** Валютный конвертер с ручным вводом курсов  
**Версия приложения для тестирования:** v1.0.0  
**Окружение:** Windows 11, Python 3.13

## Легенда статусов:
* 🟢 **Passed** (Пройден успешно)
* 🔴 **Failed** (Обнаружен дефект)
* 🟡 **Blocked** (Заблокирован другим багом)

---

## Панель тестов

| ID | Компонент / Сценарий | Шаги воспроизведения | Ожидаемый результат | Статус |
| :--- | :--- | :--- | :--- | :--- |
| **TC-01** | Меню / Выход | Выбрать пункт `0` в главном меню. | Программа выводит прощальное сообщение и корректно завершает работу. | 🟢 Passed |
| **TC-02** | Курсы / Добавление | Пункт `1` -> Из: `USD`, В: `RUB`, Курс: `90.5`. | Сообщение «Курс успешно сохранен». В БД создана запись. | 🟢 Passed |
| **TC-03** | Курсы / Обновление | Пункт `1` -> Повторно ввести `USD` -> `RUB`, но указать курс `92.0`. | Старый курс перезаписан. При конвертации будет использоваться `92.0`. | 🟢 Passed |
| **TC-04** | Валидация / Код валюты | Пункт `1` -> Ввести невалидный код (например, `US` или `123`). | Система прерывает операцию с понятной ошибкой формата кода. | 🟢 Passed |
| **TC-05** | Валидация / Пустой ввод | Пункт `1` -> Оставить поле ввода валюты пустым (нажать Enter). | Ошибка «Код валюты не может быть пустым», возврат в меню. | 🟢 Passed |
| **TC-06** | Валидация / Регистр букв | Пункт `1` -> Ввести код маленькими буквами (`usd` -> `rub`). | Программа автоматически приводит код к верхнему регистру (`USD` -> `RUB`). | 🟢 Passed |
| **TC-07** | Конвертация / Успех | Пункт `2` -> Из: `USD`, В: `RUB`, Сумма: `100` (при курсе `92.0`). | Вывод: `100 USD = 9200.0 RUB`. Операция залогирована. | 🟢 Passed |
| **TC-08** | Конвертация / Одинаковые валюты | Пункт `2` -> Из: `USD`, В: `USD`, Сумма: `100`. | Ошибка: «Исходная и целевая валюты совпадают». Расчет не происходит. | 🟢 Passed |
| **TC-09** | Конвертация / Отсутствие курса | Пункт `2` -> Попытаться перевести несуществующую пару (например, `GBP` в `JPY`). | Ошибка: «Курс обмена не найден в системе. Сначала добавьте его». | 🟢 Passed |
| **TC-10** | Валидация / Неверная сумма | Пункт `2` -> В поле суммы ввести буквы (`сто`) или отрицательное число (`-50`). | Предупреждение о некорректной сумме, расчет блокируется. | 🟢 Passed |
| **TC-11** | История / Пустая | Удалить файл БД (или на чистой БД) выбрать пункт `3`. | Программа выводит: «История операций пока пуста». | 🟢 Passed |
| **TC-12** | История / Вывод данных | Провести 2–3 конвертации, выбрать пункт `3`. | Отображается таблица со столбцами: Дата/время, Обмен, Суммы и Курс. | 🟢 Passed |

---

## Результаты стабилизации
* **Найдено критических багов:** 0
* **Итоговый статус MVP:** Готов к релизу (Версия стабильна)